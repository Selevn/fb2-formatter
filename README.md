This code changes all fb2 files in directory with names as {author} - {book-title}

1) install node.js
2) run `npm i` inside project folder
3) create `.env` file and change it as you need according to `.env.example`
4) run `node index.js`
